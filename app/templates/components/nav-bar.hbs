<div class="container-fluid">
  <div class="navbar-header logo">
    {{#if showNav}}
      <button type="button"
        class="navbar-toggle collapsed"
        aria-expanded="false"
        aria-controls="navbar" onClick={{action 'toggleCollapsed'}}>
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    {{/if}}
    <a href="{{homeUrl}}" class="navbar-brand">
      <img src="/assets/images/.png" alt="TodosConectados" />
    </a>
  </div>
  {{#if showNav}}
    {{#bs-collapse id="navbar"
      collapsed=navBarIsCollapsed
      class="collapse navbar-collapse navbar-right"}}
      <ul class="nav navbar-nav navbar-left">
        <li class="visible-xs">
         {{#link-to 'index'}}
           ¡Contrata ya!
         {{/link-to}}
        </li>
      </ul>
    {{/bs-collapse}}
  {{/if}}
</div>
