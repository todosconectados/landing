<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header logo">
      {{#if showNav}}
        <button type="button" class="navbar-toggle collapsed" aria-expanded="false" aria-controls="navbar" onClick={{action 'toggleCollapsed'}}>
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
      {{/if}}
      <a href="{{homeUrl}}" class="navbar-brand">
        <img src="/assets/images/main-logo.png" alt="GurúComm" />
      </a>
    </div>
    {{#if showNav}}
      {{#bs-collapse id="navbar" collapsed=navBarIsCollapsed class="collapse navbar-collapse navbar-right"}}
        <ul class="nav navbar-nav navbar-left">
          <li>
            {{#scroll-to href='#how-it-works' offset=-60}}
              ¿Cómo funciona?
            {{/scroll-to}}
          </li>
          <li>
            {{#scroll-to href='#work-from-home' offset=-60}}
              Trabaja desde casa
            {{/scroll-to}}
          </li>
          <li>
            {{#scroll-to href='#about' offset=-60}}
              Nuestro proyecto
            {{/scroll-to}}
          </li>
          <li>
            {{#scroll-to href='#contact' offset=-60}}
              Contacto
            {{/scroll-to}}
          </li>
          <li>
            {{#link-to "sign-up" class="btn btn-primary"}}
              Quiero mi número
            {{/link-to}}
          </li>
        </ul>
      {{/bs-collapse}}
    {{/if}}
  </div>
</nav>
